<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_background_white">

    <!-- Content Area (Where Login/Grid/SMS views are inflated) -->
    <LinearLayout
        android:id="@+id/content_container_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_above="@+id/bottom_navigation_main"/>

    <!-- Navigation Bar (Shared across all screens, but hidden on Login) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?attr/colorSurface"
        app:itemIconTint="@color/primary_teal"
        app:itemTextColor="@color/primary_teal"
        app:menu="@menu/res_menu_navigation"
        />

</RelativeLayout>